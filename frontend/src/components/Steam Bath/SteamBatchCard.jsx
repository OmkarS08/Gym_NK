import React from 'react'

const SteamBatchCard = () => {
  return (
    <div>SteamBatchCard</div>
  )
}

export default SteamBatchCard

{/* <div className="grid grid-cols-4 gap-4 mt-10">
{appointments.map((appointment) => (
  <div key={appointment.id} className="bg-white shadow-lg rounded-lg p-4">
    <h3 className="text-xl font-bold">{appointment.memberNames.join(', ')}</h3>
    <p>Date: {appointment.date}</p>
    <p>Time: {appointment.time}</p>
    <button className="mt-2 bg-red-500 text-white px-4 py-2 rounded" onClick={() => deleteAppointment(appointment.id)}>
      Delete
    </button>
  </div>
))} */}